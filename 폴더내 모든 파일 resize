# Pillow 라이브러리 불러오기
from PIL import Image
import os.path

path_dir = '/content'
 
file_list = os.listdir(path_dir)

for f in file_list:
  print(f)

  if os.path.isfile(f) :
    img = Image.open(f)
    img_resized = img.resize((50,50))
    img_resized.show()
